.PHONY: install build all run

TAG_NAME := $(shell git rev-parse --short HEAD)

install:
	npm i

build: 
	npm run build

lint:
	npm run lint

format:
	npm run format

run:
	npm run start

run-debug:
	npm run dev
